<section id="logo-section" class="text-center copyright">
  <div class="container">
    <div class="login_header">
      <ul>
        <li>
          <%if user_signed_in? %>
          	<%= link_to 'Logout', destroy_user_session_path, :method => 'delete' %>
          <% else %>
          	<%= link_to 'Login', new_user_session_path %>
          <% end %>
      </ul>
    </div>
  </div>
</section>
